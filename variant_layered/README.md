# Layered variant (kept)

Flat reference-plate / segmented-layer animation you liked.

## Files
- `scene_layered.html` — scene (also root `../scene_layered.html`)
- `capture_layered.mjs` — capture/encode
- `hyperlinks-space-sticker-layered.webm`
- `hyperlinks-space-emoji-layered.webm`

Also copied to project root as `hyperlinks-space-*-layered.webm`.

## Regenerate
```bash
node capture_layered.mjs
```
(Point the HTML URL at `scene_layered.html` if you copy this script to root.)

The newer **volumetric** pipeline is `../scene_volumetric3d.html` + `../capture_volumetric3d.mjs` → main `hyperlinks-space-sticker.webm` / `emoji`.
